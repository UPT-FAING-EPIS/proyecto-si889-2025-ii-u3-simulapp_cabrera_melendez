import 'package:flutter/material.dart';

class Sizes {
  static const double kGap20 = 20.0;
  static const double kRadius = 12.0;
  static const EdgeInsets kPadding = EdgeInsets.all(16.0);
  static const Widget kEmptyWidget = SizedBox.shrink();
}